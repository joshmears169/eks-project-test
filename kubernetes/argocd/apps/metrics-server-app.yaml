apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metrics-server
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default

  source:
    repoURL: https://kubernetes-sigs.github.io/metrics-server/
    chart: metrics-server
    targetRevision: 3.13.0
    helm:
      values: |
        # Common on EKS: kubelet certs often fail validation, so metrics-server
        # needs --kubelet-insecure-tls to scrape metrics reliably.
        args:
          - --kubelet-insecure-tls

        # Optional: keep it HA-ready (uncomment if you want)
        # replicas: 2

  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
